---
name: dept
description: Переключить активную роль проекта (variant switcher + handoff открытых задач) или создать новую роль. Use when switching a project's role/department/hat, handing off between roles, user says "dept", "смени роль", "передай в дизайн", "надень шляпу", "/dept --new".
disable-model-invocation: true
---

# Dept — переключение роли проекта

Роль проекта = симлинк `.dept.md` в его корне → `../<штаб>/departments/<role>.md`.
Скилл меняет его с полным handoff'ом. Запускается из репо проекта.

## Конфиг

Штаб и Hub Config — как в project-register. Текущая роль: `readlink .dept.md`.
Целевая роль должна существовать в `departments/` (кроме `--new`).

## /dept <role> — полный handoff

1. **Старая роль**: `readlink .dept.md` → `<old>`. Если совпадает с целевой — скажи и выйди.
2. **Write-back**: найди открытые issues `dept:<old>` этого репо
   (`gh issue list -l dept:<old> -s open`). По каждому, где есть незафиксированная
   работа — work-record-комментарий «передано в <new>: <статус одной строкой>».
   Спроси, какие закрыть.
3. **Перелинковка**: `ln -sfn ../<штаб-имя>/departments/<new>.md .dept.md` +
   коммит в проект: `chore: dept <old> → <new>`.
4. **Штаб**: обнови колонку «роль» строки проекта в HQ.md; коммит + push штаба
   (другие машины узнают о смене фазы).
5. **Задача новой роли**: предложи создать/найти issue `dept:<new>` со ссылкой
   `needs <repo>#<N>` на переданные.
6. **Подгрузи контекст СЕЙЧАС**: прочитай `departments/<new>.md` целиком — контекст-файлы
   раннер грузит только на старте сессии, поэтому с этого момента работай по
   правилам новой роли без рестарта.

## /dept <role> --quick

Только шаги 3–4 (примерить шляпу без бюрократии) + шаг 6.

## /dept --new <role>

1. Спроси домен роли одной строкой.
2. Создай `departments/<slug>.md` из `${CLAUDE_PLUGIN_ROOT}/template/departments/_role.md.tmpl`
   (покажи подходящий пример из `examples/`, если есть).
3. Лейбл `dept:<slug>` в hub-репо (в репо проектов — добавит project-register/route
   по мере надобности).
4. Коммит + push штаба. Если запущен в репо проекта — предложи сразу `/dept <slug>`.

## /dept (без аргументов)

Покажи: текущую роль проекта, список доступных ролей с доменами, открытые issues
текущей роли по проекту.

## Red flags — STOP

| Собираешься… | Вместо этого… |
|---|---|
| Сменить роль, бросив открытые issues старой без записи | Шаг 2 обязателен (кроме --quick) |
| Создать симлинк на несуществующую роль | Сначала /dept --new |
| Абсолютный путь в симлинке | Только относительный |
| Продолжить сессию по правилам старой роли | Шаг 6: прочитать новый слой сразу |
