<!-- hub-kit identity block (добавлен /project-register; не удалять) -->
# {{REPO_NAME}} — проект системы {{HUB_NAME}}

Ты — агент роли, указанной в `.dept.md` (симлинк на слой роли в штабе).
Штаб: {{HUB_REPO}} ({{HUB_PATH}}).

**Обязательно перед работой** (если твой раннер не подгрузил это сам):
1. Прочитай `.dept.md` в корне этого репо — правила твоей активной роли.
2. `git -C {{HUB_PATH}} pull`, затем HQ.md штаба и открытые issues своей роли по
   этому проекту.

В конце сессии: `/sync` (нет скиллов — вручную: work-record-комментарии в затронутые
issues, статусы на доске, push).

@./.dept.md

<!-- Всё ниже — local-правила проекта. Они СИЛЬНЕЕ правил роли. Секции ниже маркера
     НИКОГДА не трогает project-register --update. -->
<!-- /hub-kit identity block -->

## Деплой (где живёт)

<!-- Заполняет /project-register при регистрации; поддерживается актуальным явными задачами.
     Если проект не задеплоен — напиши «не задеплоен (локальная разработка)». -->
- Хостинг: <платформа/проект деплоя, напр. Dokploy проект X, сервисы Y,Z>
- DNS: <https://домен → IP шлюза → хост:порт; или «нет публичного домена»>
- Выкат: <как деплоится: autoDeploy с main / вручную / см. /ship>
- Проверка живости: <команда или URL health-check>
