# HQ — реестр проектов

Канонический список всех репозиториев системы («карта моря»). Строки добавляют скиллы
`/project-register` и `/kickoff`; активную роль переключает `/dept`. Руками не править
без явной задачи.

| роль | репо | локальный путь | домен (что делает) |
|------|------|----------------|--------------------|
| hub  | {{HUB_REPO}} | {{HUB_PATH}} | штаб: координация, память, роли |
